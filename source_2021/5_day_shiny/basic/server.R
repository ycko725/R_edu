# Load libraries, data ------------------------------------------------
library(ggplot2)
library(readr)

loan_data <- read.csv("data/train_ctrUa4K.csv")

server <- function(input, output) {
  # assign values to `output` here
}